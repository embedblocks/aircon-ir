[pytest]
# ESP-IDF convention: hardware/pytest-embedded test files are named
# pytest_*.py rather than the plain-pytest default test_*.py. This makes
# `pytest` (with no filename argument) auto-discover
# pytest_ac_ir_transport.py in this directory.
python_files = pytest_*.py
