# RX loopback GPIO for waveform capture. Jumper this pin to the ac_ir
# TX GPIO (GPIO10). Override in menuconfig under
# "AC IR Transport Test Configuration" if GPIO3 isn't free on your board.
CONFIG_AC_IR_TEST_RX_GPIO=3